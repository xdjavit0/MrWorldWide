import styled from '@emotion/styled'

export const NavigationBarContainer = styled.div`
    display: flex;
    flex-direction: row;
    width:fit-content;
    margin-left: auto;
	margin-right: auto;
`
